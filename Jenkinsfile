// Jenkinsfile in vprofile-project root
@Library('vprofile-libs') _

vprofilePipeline(
    imageName: 'sawat98/vprofile',
    gitBranch: 'master',     
    dockerCreds: 'dockercred',
    mavenTool: 'Maven'   
)
