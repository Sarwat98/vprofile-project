// Jenkinsfile in vprofile-project
@Library('vprofile-libs') _

vprofilePipeline(
    imageName: 'sawat98/vprofile',  // Required parameter
    
    // Optional parameters with defaults
    // dockerRegistry: 'docker.io'
    gitBranch: 'master
    // dockerCreds: 'dockercred'
    // mavenTool: 'Maven'
    // dockerfile: 'Dockerfile'
)
